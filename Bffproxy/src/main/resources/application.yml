server:
 port: 9090
 servlet:
    session:
      cookie:
        http-only: true
spring:
  application:
    name: Bffproxy
logging:
 level:
  org:
   springframework:
    web: debug
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
      enabled: true